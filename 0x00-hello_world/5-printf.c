#include <stdio.h>
/**
 * main - A code that displays a new line output using the printf command
 * Return: (0) (terminated)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
