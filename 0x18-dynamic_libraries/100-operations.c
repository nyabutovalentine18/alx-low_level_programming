#include <stdio.h>
#include <stdlib.h>

/**
*add - adds two elemets
*@a: first element
*@b: second element
*Return: a + b
*/
int add(int a, int b)
{
return (a + b);
}

/**
*sub - subtracts two elements
*@a: first element
*@b: second element
*Return: a - b
*/
int sub(int a, int b)
{
return (a - b);
}

/**
*mul - multiplies two elemets
*@a: first element
*@b: second element
*Return: a * b
*/
int mul(int a, int b)
{
return (a * b);
}

/**
*div_y - divides two elements
*@a: first element
*@b: second element
*Return: a / b
*/
int div_y(int a, int b)
{
return (a / b);
}

/**
*mod - finds module of two numbers
*@a: first element
*@b: second element
*Return: a % b
*/
int mod(int a, int b)
{
return (a % b);
}
